Admin route runtime verification on 2026-08-24: http://localhost:3000/admin/faq loaded with a blank white viewport. Browser console reported `[API Query Error] TRPCClientError: You do not have required permission (10002)` from `trpc.faq.adminList`. This confirms the route reaches the admin query, but the current non-admin browser session needs a clear unauthorized/forbidden UI fallback instead of a blank screen. The server restarted successfully and `pnpm check`, `pnpm build`, and `pnpm test` had passed before this browser check.
